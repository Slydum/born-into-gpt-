Born Into V7 popup hotfix

Replace these files in the repository:
- index.html
- styles.css
- src/main.js

The legacy-save warning is now a small dismissible notice inside the menu and hides when Begin a new life is selected.
